name: pr-validation

on:
  pull_request:
    branches:
    - main
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: PR Validation
        uses: actions/checkout@v4
        run: echo "PR 