name: Task_11-Matrix_CI

on:
  push:
    branches:
      - main

jons:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node: [16, 18, 20]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Testing nodes
        run: echo "NODE ${{ matrix.node}}"
